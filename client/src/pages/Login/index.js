import './Login.css';
import { Header, Input, FormBtn } from "../../components/LoginForm"

export default function Login(){
    return (
        <div>
            <Header />
            <Input />
            <FormBtn />
        </div>
    )
}